import WantToBuild from "../components/main/components/WantToBuild";
import MainContainer from "../components/MainContainer";


const work = () => {
  return (
    <MainContainer keywords="konstrut work" title="work">
      <WantToBuild />
    </MainContainer>
  );
};

export default work;
