import Contacts from "../components/Contacts";
import MainContainer from "../components/MainContainer";

const contacts = () => {
  
  return (
    <MainContainer keywords="konstrut contacts" title="contacts">
      <Contacts />
    </MainContainer>
  );
};

export default contacts;
